package gregtech.api.capability.impl;

/*import gregtech.api.capability.IControllable;
import gregtech.api.capability.IEnergyContainer;
import gregtech.api.capability.IMaintenable;
import gregtech.api.metatileentity.MetaTileEntity;
import gregtech.api.recipes.RecipeMap;

import java.util.Collection;
import java.util.function.Supplier;

public class MaintenanceLogic extends RecipeLogicEnergy implements IControllable, IMaintenable {

    public MaintenanceLogic(MetaTileEntity tileEntity, RecipeMap<?> recipeMap, Supplier<IEnergyContainer> energyContainer, int maintenanceLevel)

    private boolean workingEnabled = true;
    private boolean maintained = false;

    @Override
    public boolean isWorkingEnabled() {
        return workingEnabled;
    }

    @Override
    public void setWorkingEnabled(boolean workingEnabled) {
        this.workingEnabled = workingEnabled;
        metaTileEntity.markDirty();
    }

    protected void resetWorking() {
        this.canWork = false;
    }

}*/
